@page "/Announcements/{id:guid}/Edit"
@model TemperedTyrant.CreatorToolkit.Web.Pages.Announcements.EditModel
@using TemperedTyrant.CreatorToolkit.Web.Announcements
@{
    ViewData["Title"] = "Edit announcement";
}
<header class="page-heading">
    <h1>Edit announcement</h1>
    <p class="lede">Update the message and encrypted draft images.</p>
</header>
@if (Model.HasConflict)
{
    <section class="notice" role="alert">
        <p><a asp-page="/Announcements/Edit" asp-route-id="@Model.Id">Reload the current draft</a> in a new request. Copy any entered changes you want to keep first.</p>
    </section>
}
<form method="post" enctype="multipart/form-data" class="card editor-form">
    <input asp-for="Revision" type="hidden">
    <div asp-validation-summary="ModelOnly"></div>
    @await Html.PartialAsync("_AnnouncementComposer", new AnnouncementComposerViewModel(Model.Title, Model.MessageContent, Model.ExistingMedia, true, true, Model.Id))
    <div class="actions">
        <button type="submit">Save changes</button>
        <a class="button secondary" asp-page="/Announcements/Details" asp-route-id="@Model.Id">Cancel</a>
    </div>
</form>
